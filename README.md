React elements for native apps
===

Using
---

```import cmp, {sts} from 'ambl';```

- ```cmp``` contains all elements
- ```sts``` contains methods to work with stylesheets


There is a library with similar elements for web apps: ```aweb```